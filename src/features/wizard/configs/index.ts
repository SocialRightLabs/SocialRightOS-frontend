import homeCareConfig from './homeCareConfig';
import cozgerHomeCareConfig from './cozgerHomeCareConfig';
import disabilityPensionConfig from './disabilityPensionConfig';
import birthGrantConfig from './birthGrantConfig';
import gssConfig from './gssConfig';
import oldAgePensionConfig from './oldAgePensionConfig';

const configs = {
  homeCare: homeCareConfig,
  cozgerHomeCare: cozgerHomeCareConfig,
  disabilityPension: disabilityPensionConfig,
  birthGrant: birthGrantConfig,
  gss: gssConfig,
  oldAgePension: oldAgePensionConfig
};

export {
  homeCareConfig,
  cozgerHomeCareConfig,
  disabilityPensionConfig,
  birthGrantConfig,
  gssConfig,
  oldAgePensionConfig
};

export default configs;
